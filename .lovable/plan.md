## Update Main Floor Tile price — bal-ftile-01

**File:** `src/data/tiered-catalog.ts` (line 400)

**Change:**
- `price: 850` → `price: 10.11`

No other fields touched. Other two Main Floor Tile products (`bal-ftile-02`, `bal-ftile-03`) remain unchanged pending your prices for those.

**After applying:**
- Preview-only change (no publish needed for testing).
- Will be live once you click Publish.
- Test: visit `/shop`, scroll to Main Floor Tile → "Dignitary 12\" x 24\" Rectangle Floor and Wall Tile" should show **$10.11**.